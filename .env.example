# Copy to .env for local dev. On Netlify, set these under Site configuration > Environment variables.
NUXT_RESEND_API_KEY=
NUXT_CONTACT_TO_EMAIL=info@corescale.dev
# Use an address on a domain verified in Resend (e.g. Corescale Website <noreply@corescale.dev>)
NUXT_CONTACT_FROM_EMAIL=Corescale Website <onboarding@resend.dev>
